# SQLite database files — regenerated by running the lesson scripts
*.db
*.db-journal
*.db-wal
*.db-shm

# Local Claude Code settings (machine-specific, not useful to share)
.claude/settings.local.json

# Node
node_modules/
npm-debug.log*

# Prisma-generated client (regenerated by `prisma generate` / `prisma migrate dev`)
**/node_modules/.prisma/
